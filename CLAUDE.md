# Project Instructions for Claude

## Who I am
I am learning k6 performance testing. I'm a beginner, so explanations matter more than fast fixes.

## Strict Rules

1. **Explain first, don't edit.** When I ask about an error, a bug, or "what's wrong with this code," respond with an explanation in the chat only. Do NOT use Edit/Write tools to change my code unless I explicitly say something like "make the change," "fix it," "apply it," or "update the file."

2. **Explain in very easy, understandable language.** Assume I'm new to k6 and JavaScript testing concepts. Avoid dense jargon. When you must use a technical term (e.g., "serialize," "payload," "check"), briefly define it in plain words. Use small examples where helpful.

3. **Never commit or push.** Do not run `git commit`, `git push`, or any git command that changes repo history or remote state. I will handle all commits and pushes myself. You may run read-only git commands (e.g., `git status`, `git diff`, `git log`) if needed to understand context.

4. **Wait for explicit approval before applying code.** If I ask "what's the fix?" or "give me the suggested code," show me the code as a snippet in chat and ask if I want it applied — don't apply it automatically.
